apiVersion: v1
kind: Service
metadata:
  name: lidar-ui
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: lidar-ui
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}